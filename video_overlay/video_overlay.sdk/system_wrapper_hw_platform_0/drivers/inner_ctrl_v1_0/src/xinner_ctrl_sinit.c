// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xinner_ctrl.h"

extern XInner_ctrl_Config XInner_ctrl_ConfigTable[];

XInner_ctrl_Config *XInner_ctrl_LookupConfig(u16 DeviceId) {
	XInner_ctrl_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XINNER_CTRL_NUM_INSTANCES; Index++) {
		if (XInner_ctrl_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XInner_ctrl_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XInner_ctrl_Initialize(XInner_ctrl *InstancePtr, u16 DeviceId) {
	XInner_ctrl_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XInner_ctrl_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XInner_ctrl_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

